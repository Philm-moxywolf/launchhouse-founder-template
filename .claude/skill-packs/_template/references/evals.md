# {{name}} — behaviour scenarios

At least 8 scenarios, at least 3 of them refusals or guard-rail cases (cold send, spend, bulk, starting a sequence, the other track). Tag every guard-rail scenario's heading with a literal `(guard rail)` suffix; the validator counts those, not the wording of the scenario itself.

### 1. {{short name}}

Founder says: "{{...}}"

Expected: {{what Claude / the specialist does, which tools, where it stops for a yes, what it refuses}}

### 2. {{short name}} (guard rail)

Founder says: "{{...}}"

Expected: {{what Claude refuses, and why, in the founder's own words}}
